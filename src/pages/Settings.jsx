export default function Settings() {
    return (
        <section className="p-6 rounded-lg bg-[var(--bg-surface)] border border-[var(--border)]">
            <h2 className="font-semibold text-xl mb-2">Settings</h2>
            <p className="text-[var(--text-muted)]">This is the Settings child route, rendered inside the parent's Outlet.</p>
        </section>
    );
}
