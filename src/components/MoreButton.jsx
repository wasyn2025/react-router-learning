export default function MoreButton({text}) {
    return (
        <button className="cursor-pointer bg-purple-200 text-zinc-900 rounded-full py-2 px-4 text-sm">
            {text}
        </button>
    );
}